## service语法块

```
@server(
  jwt: Auth
  group: foo
  middleware: AuthMiddleware
  prefix: api
)
service foo-api{
  @doc "foo"
  @handler foo
  post /foo/:id (Foo) returns (Bar)
}

service foo-api{
  @handler ping // 
  get /ping
  
  @doc "foo"
  @handler bar
  post /bar/:id (Foo)
}

```

