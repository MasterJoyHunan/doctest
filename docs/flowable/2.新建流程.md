### 代码

```java
@Autowired
private RepositoryService repositoryService;

public void deploy() {
    Deployment deploy = repositoryService.createDeployment()
        .addClasspathResource("holiday-request.bpmn20.xml")
        .deploy();
    log.info("{}", deploy);
}
```

### SQL

```sql
INSERT INTO `flow`.`act_re_procdef` ... 流程定义 process-definitions -- 重点
INSERT INTO `flow`.`act_re_deployment` ... 流程部署
INSERT INTO `flow`.`act_ge_bytearray` ... 流程的 `xml` 文件
```

